server:
  port: 9000
spring:
  application:
    name: userService
  profiles:
    active: dev
  cloud:
    nacos:
      config:
        file-extension: yml
        server-addr: ${fo.pansky.com}:8848
logging:
  level:
    com.pansky: debug

fo:
  pansky:
    com: 127.0.0.1
